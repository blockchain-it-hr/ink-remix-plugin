apiVersion: v1
kind: Service
metadata:
  name: server-svc
  labels:
    namespace: ink
    app: ink
    component: server
spec:
  type: ClusterIP
  selector:
    app: ink
    component: server
  type: 
  ports:
  - name: server-port
    protocol: TCP
    port: 80
    targetPort: 65520
